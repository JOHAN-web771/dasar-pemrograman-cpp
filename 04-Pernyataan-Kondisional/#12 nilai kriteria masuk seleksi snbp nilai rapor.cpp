#include <iostream>
using namespace std;
 int main ()
 {
  	int  nilaiRata;
  	cout << "\n*** Program Sederhana Seleksi SNBP 2025 ***\n";
 	cout <<"Masukkan nilai rata anda (harus dibulatkan): ";cin>> nilaiRata;
 	
 	if (nilaiRata <= 50) {
 		cout << "anak anda tidak masuk seleksi SNBP";
	 } else if (nilaiRata <= 60) {
 		cout << "anak anda tidak masuk seleksi SNBP";
	 }  else if (nilaiRata <= 70) {
 		cout << "anak anda tidak masuk seleksi SNBP";
	 }  else if (nilaiRata <= 80) {
 		cout << "anak anda masuk seleksi SNBP";
	 }  else if (nilaiRata <= 95) {
 		cout << "anak anda masuk seleksi SNBP";
 } else {
 	cout<<"data tidak ditemukan";
 }
 cout << "\nTerimakasih Telah Menggunakan Layanan Dari Kami ;]"<<endl;
 cout << "--------------------------------------------------";
	
 
 }
